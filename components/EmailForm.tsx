
// This component is no longer used as email handling is now done by MailerLite.
// Keeping the file to avoid breaking imports if it were part of a larger system,
// but its functionality is removed. For a new setup, this file could be deleted.
export {};
